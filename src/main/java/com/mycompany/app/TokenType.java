package com.mycompany.app;

/** Enum for token types. */
enum TokenType {
  // 1 or 2 character tokens
  LEFT_PAR,
  RIGHT_PAR,
  EQUALS,
  GREATER_THAN,
  GREATER_OR_EQ,
  LESS_THAN,
  LESS_THAN_OR_EQ,
  SEMICOLON,
  COMMA,
  PLUS,
  MINUS,
  STAR,
  SLASH,

  // literals
  IDENTIFIER,
  STRING_LITERAL,
  STRING_TYPE,
  NUMBER,

  // keywords
  SELECT,
  INSERT,
  DELETE,
  FROM,
  WHERE,
  AND,
  OR,
  NOT,
  IN,
  CREATE,
  TABLE,
  PRIMARY,
  FOREIGN,
  REFERENCES,
  UNIQUE,
  JOIN,
  ON,
  AS,
  ORDER_BY,
  GROUP_BY,
  HAVING,
  LIMIT,
  UPDATE,
  SET,
  VALUES,
  ALTER,
  ADD,
  DROP,
  RENAME,
  COLUMN,
  DATABASE,
  IF,
  EXISTS,
  KEY,
  NULL,
  SHOW,
  INTO,

  EOF
}
